    <div class="jumbotron">
        <h1>Bem-vindo ao Blog do node</h1>
        <p>This is a simple hero unit, a simple jumbotron-style component for calling extra attention to featured content or information.</p>
        <p><a class="button" href="#">Crie uma conta</a></p>
    </div>
<hr>
<h2>Postagens recentes:</h2>

    {{#each postagens}}
    <div class="card">
        <div class="card-body">
            <h4>{{titulo}}</h4>
            <p>{{descrição}}</p>
            <a href="/postagens/{{slug}}">
                <button class="btn btn-success">Leia mais</button>
            </a>
            <hr>
            <small>Nome da categoria:{{categoria.nome}}</small>
            <hr>
            <small>Data de publicação {{categoria.date}}</small>
        </div>
    </div>

    {{else}}
    <h5>Nenhuma postagem registrada!</h5>
    {{/each}}